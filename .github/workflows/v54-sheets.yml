name: v54 sheets and white-screen fix
on:
  workflow_dispatch:
jobs:
  apply:
    runs-on: ubuntu-latest
    if: false
    permissions:
      contents: write
    steps:
      - run: echo "v54 already applied — disabled so it does not fight later updates"
